- content_for :title do
  Your favorite fighters
%h2
  Favorite Fighters
  .action
    = button_to 'New Fighter', new_members_fighter_path, {:method => :get, :class => "bright"} 
%p
  = "#{pluralize(@fighters.count,'fighters')} as of #{Time.now.strftime("%b %d, %Y")}"
.fighters
  = fighters_list(@fighters)
