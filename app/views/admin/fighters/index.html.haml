- content_for :title do
  All Fighters
%h2
  All Fighters
  .action
    = button_to 'New Fighter', new_admin_fighter_path, {:method => :get, :class => "bright"} 
%p
  = "#{pluralize(Fighter.count,'fighters')} as of #{Time.now.strftime("%b %d, %Y")}"
.fighters
  = fighters_list(@fighters)
